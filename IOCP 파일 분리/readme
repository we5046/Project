1차 버전입니다.

Network part
-> IOCP와 WSA, main함수가 이곳에 있습니다.
MultiThread를 활용하여 IOCP에 처리완료된 WSARecv, WSASend를 가져옵니다.

Packet part
최소한의 Packet Header 검증을 합니다.

Protocol part
IOCP 네트워크와 게임서버 사이 어댑터 역할을 하는 함수가 존재하는 공간입니다. 게임 로직을 보호하며 네트워크 디펜스 레이어 역할을 합니다.

RingBuffer
Session의 Recv data를 담을 공간입니다. Stream 형태의 데이터를 상대방이 얼마나 보낼지 알 수 없기 때문에, 최적의 역할을 해 줄 수 있는 것이 RingBuffer라고 생각했습니다.
(Send의 경우 Recv와 달리, 보내는 크기를 알기 때문에 자료구조 deque를 이용해 빠른pop_front가 가능하게 했습니다.)

Session part
Session 관련 변수, 함수들이 포진되어있습니다. 
현재는 OnRecvComplete에서 RingBuffer에 들어있는 패킷들을 조립하여 완전체 Packet으로 만듭니다.

[ Client ]
    |
    | TCP Stream
    v
[ Network.cpp ]
    |
    | WSARecv / WSASend 등록
    | IOCP Completion 통지
    v
[ WorkerThread (IOCP) ]
    |
    | recv completion / send completion
    v
[ Session ]
    |
    | RingBuffer (스트림 처리)
    | Packet 조립
    v
[ Protocol ]
    |
    | Packet 검증 (Network Defense)
    v
[ GameServer ]
    |
    | 게임 규칙 처리 (채팅, 룸 등)
